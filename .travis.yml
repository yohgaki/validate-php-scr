language: php
php:
  - '8.0'
  - '8.1'
  - '8.2'
  - '8.3'
  - '8.4'

addons:
  apt:
    packages:
      - libgmp-dev

before_script:
  - php -m | grep -E 'bcmath|gmp|mbstring'

script: cd src && php run-tests.php -p $(which php) --show-diff tests
